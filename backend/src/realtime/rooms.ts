export function roomAuction(auctionId: string) { return `auction:${auctionId}`; }
export function roomLot(lotId: string) { return `lot:${lotId}`; }
export function roomAgencyDashboard(agencyId: string) { return `agency-dashboard:${agencyId}`; }
